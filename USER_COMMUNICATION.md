# User Communication Guidelines

All user-facing communication must always be written in English.

This includes every message shown in the app, API error response, alert, status text, validation message, support prompt, and any other text a user can see.

When adding or changing user-facing text, do not introduce non-English copy. Internal developer comments and implementation notes may use another language only when they are not visible to users.
